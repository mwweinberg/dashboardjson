# dashboardjson
creates the JSON file for the chinafile dashboard


Looks for an input file called "dashboardinput.csv" and outputs "dashboardoutput.json".  The output is intended to be manally cut and pasted into the HTML for the dashboard page.  It creates headings based on the input file so it can be used for country and province level dashboards.
